<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.DeviceDefault.QuickSettings" parent="@*android:style/Theme.DeviceDefault">
        <!-- QS / volume panel background -->
        <item name="android:colorBackgroundFloating">@color/qs_panel_background</item>

        <!-- qs tile enabled icon color -->
        <item name="android:colorPrimary">@color/qs_tile_color</item>

        <!-- i srsly have no clue, but as long as this color is defined as no matter what,
        the qs panel background is transparent and not @color/black -->
        <item name="android:navigationBarColor">@android:color/transparent</item>

    </style>
</resources>